# Base URL for API requests (include /api suffix if backend expects it)
VITE_API_URL=https://api.kickoffhub.space/api

# Optional override for legacy configs
VITE_API_BASE_URL=

# Development proxy target (used by Vite dev server)
VITE_DEV_PROXY_TARGET=http://localhost:3000
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173

# App specific settings
VITE_APP_NAME=KickOffHub
